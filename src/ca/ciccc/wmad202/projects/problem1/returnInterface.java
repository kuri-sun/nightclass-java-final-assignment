package ca.ciccc.wmad202.projects.problem1;

import java.util.ArrayList;

// this makes interface for anonimous function and lambda expression ( for innerclass's instance created ).
public interface returnInterface {

  public Integer returnInt(ArrayList<Integer> list);

}
